<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- followed recommended change from last reviewer but setting width below to match_parent and adding adjustViewBounds="true"
    to activity_main doesn't seem to result in desired effect (movie posters enlarged in landscape view) and can't figure out why -->

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:id="@+id/iv_item_movie"/>

</FrameLayout>